export default function modal() {
    /*let modalId = document.getElementById('modal');
    let logoutId = document.getElementById('logout');
    logoutId.addEventListener('click', () => {
        modalId.setAttribute('visibility', 'visible')
    })*/
}